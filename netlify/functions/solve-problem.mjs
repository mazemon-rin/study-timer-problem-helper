import { GoogleGenAI } from "@google/genai";
const MODEL=process.env.GEMINI_MODEL||"gemini-2.5-flash";
const prompt=`高校生向けの学習支援AIです。画像の問題を慎重に読み取り、読めない情報は推測せず、必ずJSONだけで返してください。形式: {"readable":true,"subject":"","topic":"","problemText":"","missingInformation":[],"hint":"","steps":[],"finalAnswer":"","explanation":"","confidenceNote":""}`;
export default async request=>{if(request.method!=="POST")return new Response("Method Not Allowed",{status:405});try{const body=await request.json();if(!body.image||body.image.length>8_000_000)return Response.json({error:"画像が大きすぎます"},{status:413});const ai=new GoogleGenAI({apiKey:process.env.GEMINI_API_KEY});const result=await ai.models.generateContent({model:MODEL,contents:[{text:prompt+` 科目:${body.subject||"その他"}`},{inlineData:{mimeType:"image/jpeg",data:body.image.split(",")[1]}}]});const text=result.text.replace(/^```json\s*|\s*```$/g,"").trim();return Response.json(JSON.parse(text))}catch{ return Response.json({error:"問題を解析できませんでした"},{status:500})}};
